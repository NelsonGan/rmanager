CREATE TABLE leave(
    Leave_ID int(20) AUTO_INCREMENT,
    FOREIGN KEY (Staff_ID) REFERENCES staff(Staff_ID),
    leavedate date,
    reason varchar(500),
    status varchar(30),
    PRIMARY KEY(Leave_ID)
);